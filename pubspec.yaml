name: lumina
description: Lumina tribute capsule platform
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.8
  file_picker: ^6.0.0
  flutter_riverpod: ^2.4.0
  go_router: ^12.0.0
  http: ^1.1.0
  universal_html: ^2.2.4
  video_player: ^2.8.2
  just_audio: ^0.9.36
  url_launcher: ^6.2.6

flutter:
  uses-material-design: true  